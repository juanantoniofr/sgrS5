{% extends 'base.html.twig' %}

{% block title %}SgrTitulacion{% endblock %}

{% block body %}
    <h1>Titulación</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sgr_titulacion.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ sgr_titulacion.codigo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ sgr_titulacion.nombre }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ sgr_titulacion.tipo }}</td>
            </tr>
        </tbody>
    </table>
    <div class="mb-4">
        
        <a class="btn btn-primary mb-2" type="button" href="{{ path('sgr_titulacion_edit', {'id': sgr_titulacion.id}) }}">Editar</a><br />
        <a href="{{ path('sgr_titulacion_index') }}">Volver a la lista</a>
    </div>
    {{ include('sgr_titulacion/_delete_form.html.twig') }}
{% endblock %}
