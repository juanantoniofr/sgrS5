{% extends 'base.html.twig' %}

{% block title %}SGR: Titulaciones{% endblock %}

{% block body %}
    <h1 class="mb-2">Titulaciones</h1>

    <table class="table table-striped">
        <thead class="thead-dark">
            <tr>
                
                <th>Codigo</th>
                <th>Nombre</th>
                <th>Tipo</th>
                <th>Asignaturas</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sgr_titulacion in sgr_titulacions %}
            <tr>
                
                <td>{{ sgr_titulacion.codigo }}</td>
                <td>{{ sgr_titulacion.nombre }}</td>
                <td>{{ sgr_titulacion.tipo }}</td>
                <td>{{ sgr_titulacion.asignaturas|join(', ',' y ')}}</td>
                <td>
                    <a class="btn btn-secondary m-1" type="button" href="{{ path('sgr_titulacion_show', {'id': sgr_titulacion.id}) }}">Ver</a>
                    <a class="btn btn-primary m-1" type="button" href="{{ path('sgr_titulacion_edit', {'id': sgr_titulacion.id}) }}">Editar</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">No hay titulaciones definidas</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-primary" type="button" href="{{ path('sgr_titulacion_new') }}">Nueva titulación</a>
{% endblock %}