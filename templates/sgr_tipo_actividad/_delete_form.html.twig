<form method="post" action="{{ path('sgr_tipo_actividad_delete', {'id': sgr_tipo_actividad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sgr_tipo_actividad.id) }}">
    <button class="btn">Delete</button>
</form>
