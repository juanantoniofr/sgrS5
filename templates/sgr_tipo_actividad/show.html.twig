{% extends 'base.html.twig' %}

{% block title %}Sgr: Actividades{% endblock %}

{% block body %}
    <h1>Actividad</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sgr_tipo_actividad.id }}</td>
            </tr>
            <tr>
                <th>Actividad</th>
                <td>{{ sgr_tipo_actividad.actividad }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ sgr_tipo_actividad.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mb-4">

        <a class="btn btn-primary" type="button" href="{{ path('sgr_tipo_actividad_edit', {'id': sgr_tipo_actividad.id}) }}">Editar</a><br />
        <a href="{{ path('sgr_tipo_actividad_index') }}">Volver a la lista</a>
    </div>
    
    {{ include('sgr_tipo_actividad/_delete_form.html.twig') }}
{% endblock %}
