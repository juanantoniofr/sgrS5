<form class="float-right" method="post" action="{{ path('sgr_asignatura_delete', {'id': sgr_asignatura.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sgr_asignatura.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
