{% extends 'base.html.twig' %}

{% block title %}SgrEspacio{% endblock %}

{% block body %}
    <h1>Espacio</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sgr_espacio.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ sgr_espacio.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ sgr_espacio.descripcion }}</td>
            </tr>
            <tr>
                <th>Aforo Máximo</th>
                <td>{{ sgr_espacio.aforo }}</td>
            </tr>
            <tr>
                <th>Aforo Examen</th>
                <td>{{ sgr_espacio.AforoExamen }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mb-4">

        <a class="btn btn-primary mb-2" type="button" href="{{ path('sgr_espacio_edit', {'id': sgr_espacio.id}) }}">Editar</a><br />
        <a href="{{ path('sgr_espacio_index') }}">Volver a la lista</a>
    </div>

    {{ include('sgr_espacio/_delete_form.html.twig') }}
{% endblock %}
