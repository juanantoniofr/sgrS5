<form method="post" action="{{ path('sgr_evento_delete', {'id': sgr_evento.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sgr_evento.id) }}">
    <button class="btn">Delete</button>
</form>
