<form class="float-right" method="post" action="{{ path('sgr_taxonomia_delete', {'id': sgr_taxonomium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sgr_taxonomium.id) }}">
    <button class="btn btn-danger" role="button">Delete</button>
</form>
