{% extends 'base.html.twig' %}

{% block title %}SgrProfesor{% endblock %}

{% block body %}
    <h1>SgrProfesor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sgr_profesor.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ sgr_profesor.nombre }}</td>
            </tr>
            <tr>
                <th>Dni</th>
                <td>{{ sgr_profesor.dni }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sgr_profesor_index') }}">back to list</a>

    <a href="{{ path('sgr_profesor_edit', {'id': sgr_profesor.id}) }}">edit</a>

    {{ include('sgr_profesor/_delete_form.html.twig') }}
{% endblock %}
