{% extends 'base.html.twig' %}

{% block title %}SgrProfesor{% endblock %}

{% block body %}
    <h1>Profesor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sgr_profesor.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ sgr_profesor.nombre }}</td>
            </tr>
            <tr>
                <th>Dni</th>
                <td>{{ sgr_profesor.dni }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mb-4">
        
        <a class="btn btn-primary mb-2" role="button" href="{{ path('sgr_profesor_edit', {'id': sgr_profesor.id}) }}">Editar</a><br />
        <a href="{{ path('sgr_profesor_index') }}">Volver a la lista</a>
    </div>
    

    {{ include('sgr_profesor/_delete_form.html.twig') }}
{% endblock %}
